package constants;

public class Enum {
    public enum MAIN_MENU{
        SHOW_INFORMATION,
        ADD_ATM_ACCOUNT,
        ADD_CREDIT_ACCOUNT,
        WITHDRAW,
        SHOW_TRANSACTION_HISTORY,
        EXIT
    }
}
